{
  "framework": "nextjs",
  "buildCommand": "pnpm --filter @staffd/agents build && pnpm --filter web build",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/worker/tick",
      "schedule": "0 6 * * *"
    }
  ]
}

import DepartmentRoom from "../../components/DepartmentRoom";

export default function DesignPage() {
  return (
    <DepartmentRoom
      department="design"
      icon="🎨"
      title="Design"
      tagline="Brand identity, visual direction, UI/UX, and AI image prompts — your creative team."
      placeholder="e.g. 'brand guidelines for my business' or 'image prompt for a hero banner'…"
    />
  );
}
